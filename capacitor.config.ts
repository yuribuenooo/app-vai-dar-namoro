import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vai-dar-namoro-tb',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
